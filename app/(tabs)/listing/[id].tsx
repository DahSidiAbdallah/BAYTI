// Duplicate listing route under (tabs).
// Intentionally a no-op stub: return null so the tab router won't render a UI from this file.
// This file should be deleted; keeping a soft stub prevents transient blank screens while delete is performed.
import React from 'react';

export default function ListingStub() {
  return null;
}
